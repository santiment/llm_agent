# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Environment / secrets
.env
.venv/

# uv
.uv/

# Deployment-specific drop-in tools (loaded at runtime, not committed)
# custom_tools/
.DS_Store

# langgraph dev local runtime state (checkpoints, ops, vector store — may hold
# conversation data; never commit)
.langgraph_api/
